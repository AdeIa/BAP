<nav class = "navbar navbar-default nav-custom">
  <div class="container-fluid"> 

   <div class="navbar-header pull-right custom-navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" 
         data-target="#user-navbar-collapse">
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
         <span class="icon-bar"></span>
      </button>
     <div class="navbar-brand account">
      <%= render 'layouts/userAccount' %>   
     </div>
   </div>
    <div class="collapse navbar-collapse" id="user-navbar-collapse">

      <ul class="nav navbar-nav menu">
      <li>
        <%= link_to home_index_path, :class => "nav-href home #{is_active?(home_index_path)}" do %>
          <img src="/images/home.png" height="30px" alt="home" >
        <% end %>
      </li>
      <li class="dropdown">
          <a href="#" class="dropdown-toggle nav-href <%= is_active?(loans_user_path(:id => current_user.id)) %> <%= is_active?(loans_user_history_path(:id => current_user.id)) %>" data-toggle="dropdown" role="button" aria-expanded="false">Vypůjčené knihy<span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li>
              <%= link_to loans_user_path(:id => current_user.id) do %>
                Aktuální výpůjčky
              <% end %>
            </li>
             <li>
              <%= link_to loans_user_history_path(:id => current_user.id) do %>
                Historie výpůjček
              <% end %>
            </li>
          </ul>
        </li>        
        <li>
          <%= link_to reservations_user_path(:id => current_user.id), :class => "nav-href #{is_active?(reservations_user_path(:id => current_user.id))}" do %>
            Rezervace
          <% end %>
        </li>
        <li><%= link_to books_path, :class => "nav-href #{is_active?(books_path)}" do %>Katalog knih<% end %></li>
        <li>
            <%= link_to contact_path, :class => "nav-href #{is_active?(contact_path)}" do %>
                  Kontakt
                <% end %>
        </li>
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

